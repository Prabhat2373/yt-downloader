import GetInTouchContainer from "@/containers/app/contact/GetInTouchContainer";
import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact US | Fast4k",
  description: "Support for Fast4k",
};
const Page = () => {
  return <GetInTouchContainer />;
};

export default Page;
